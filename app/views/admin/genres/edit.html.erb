<h1>ジャンル編集</h1>
<%= form_with model: @genre,url: admin_genre_path, method: :patch, local: true do |f| %>
  <div class="form-group">
    <%= f.label "ジャンル名" %>
    <%= f.text_field :name %>
  </div>
  <div class>
    <%=f.submit "変更を保存" %>
  </div>
<% end %>